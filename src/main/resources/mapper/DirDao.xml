<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dirTraversal.dao.DirDao">

    <insert id="insertDir" parameterType="dir">
        insert into dir_inf(id, path, isDir,length,mod_Time,owner)
        values(#{id}, #{path}, #{isDir},#{length},#{mod_Time},#{owner})
    </insert>

</mapper>